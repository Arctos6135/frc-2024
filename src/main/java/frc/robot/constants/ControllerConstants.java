package frc.robot.constants;

/**
 * This class keeps track of everything related to the XBox Controllers.
 */
public class ControllerConstants {
    // Controller deadzone to manage drift.
    public static final double CONTROLLER_DEADZONE = 0.15;

    // The ports that the XBox controllers are plugged into
    public static int DRIVER_CONTROLLER = 0;
    public static int OPERATOR_CONTROLLER = 1;
}